export type NftData = {
  title: string;
  tokenId: string;
};

export type RedemptionType = "pickup" | "delivery" | "";

export type CompanyInfo = {
  name: string;
  website: string;
  image: string;
  description: string;
};
